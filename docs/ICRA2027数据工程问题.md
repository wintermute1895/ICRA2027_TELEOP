1.数据采集的启动方式：在对应环境和代码目录下开三个终端。一个终端运行：
```bash
  # 真机数采
  bash scripts/start_capture_session.sh \
    --real --physical-estop-ready \
    --confirm=I_UNDERSTAND_REAL_ROBOT

  # O6 手控制
  bash scripts/start_hand_control_session.sh \
    --physical-estop-ready \
    --confirm=I_UNDERSTAND_REAL_HAND

  # correction + SigLIP2 训练数据准备
  bash scripts/prepare_filter_training_view.sh \
    --config=config/pipeline/filter_training_view.yaml

  稳定参数分别集中在：

  - config/capture_session.env
  - config/hands/o6_control.env
  - config/pipeline/filter_training_view.yaml
```
另外两个终端分别用来进入tmux会话和停止tmux会话
```bash
tmux attach -t teleop_capture
```

```bash
bash scripts/stop_capture_session.sh
```


2.数据的保存位置：
配置文件在config/capture_session.env，路径的顺序是
```bash
  路径由 scripts/start_capture_session.sh:308 决定：

  RUN_ROOT="${RUNEVIDENCE_ROOT:-$ROOT_DIR/evidence/teleop}"
  [[ -n "$DATA_ROOT" ]] && RUN_ROOT="$DATA_ROOT"

  优先级是：

  1. 命令行 --data-root=...
  2. 配置文件中的 CAPTURE_DATA_ROOT
  3. 环境变量 RUNEVIDENCE_ROOT
  4. 默认路径 $ROOT_DIR/evidence/teleop
```

3.数据的迁移和格式转换方式：
格式转换：rosbag2lerobot ，脚本在。使用方式：两个文件路径作为参数
数据迁移 脚本在：。使用方式：文件名/路径和目标路径作为参数

4.VLM的使用方式:

千问模型的下载位置：`/media/ilex/Cyan_data/ICRA2027_MODELS/huggingface/models/Qwen2.5-VL-3B-Instruct`

SigLIP2的下载位置：/media/ilex/Seagate Hub/ICRA2027_DATA_TASK2/vlm_cache/models--google--siglip2-base-patch16-224/snapshots/75de2d55ec2d0b4efc50b3e9ad70dba96a7b2fa2
``
5.操作员标注方式:
```bash
  实时双人标注已接入采集会话：

  - 新增 tools/audit_event_recorder.py
  - 启动器自动创建 annotate tmux 窗口
  - 审计员数字键无需 Enter，立即生效：

    1 approach、2 align、3 short_insert、4 correction start/end、5 stalled、6 recovery、7target_lost、8 retreat、9 success、0 failure

  - 每条事件记录：
      - ROS 时间戳
      - monotonic 时间戳
      - wall-clock 时间戳
      - episode ID
      - auditor ID
      - sequence
      - 事件类型和 payload

  - 事件同时写入：
      - /teleop/events
      - artifacts/audit_events.jsonl

  - 只有当前 episode 正在录制时才接受标注，避免写入上一条数据。
  - 导出器和 canonical 转换器现在会保留这些事件到 streams/events.jsonl。

  启动后进入审计窗口：

  tmux select-window -t teleop_capture:annotate

  审计员 ID 可以配置：

  CAPTURE_AUDITOR_ID=auditor_01

  或：

  bash scripts/start_capture_session.sh --auditor-id=auditor_01 ...
```

6.控制手的方式：
1.脚本在tools/hand_gesture_player.py
2.配置在gestures/o6_gestures.json
3.环境在

7.修复了关节限位问题：在hardware_teleop.yaml的RS/LS配置里，实验室机器人应该配置为LS

8.VLM自动审计/实时标注的部署：
审计/离线标注代码：tools/qwen_visual_audit.py
离线关键帧抽取代码：tools/extract_rosbag_keyframes.py
SigLIP2 embedding 代码 :tools/encode_images_with_vlm.py
embedding 接入滤波训练视图 :tools/attach_vlm_embeddings.py
实时部署标注的代码（未上线 实验中）：
审计结果的本地保存路径：reports/vlm_audit/20260901_qwen25vl_3b
审计结果的移动硬盘保存路径：缺对应工具
