# Legacy

This directory preserves superseded task-scene utilities and dated handoff notes.
They are not supported collection, training, or simulation entry points. Use the
root `README.md`, `TELEOP_QUICKSTART.md`, `docs/DATA_PIPELINE.md`, and the
USB-C launcher under `scripts/` for current workflows.
