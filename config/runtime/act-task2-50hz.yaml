schema: robot_teleop.act-runtime/v1
enabled: true
checkpoint: ${TELEOP_DATA_ROOT}/act_checkpoints/task2_button_press_50hz_20260908T171908Z/checkpoints/466900/pretrained_model
checkpoint_sha256: 8ed6d32a7300bd8d5696eec42047522a830bee3d8ee6c7ab76c4ac6ab46f60cb
device: cuda
socket: /tmp/teleop_act.sock
inference_hz: 10.0
# The checkpoint was trained with a 100-step queue at 50 Hz. At this 10 Hz
# control rate, execute only two steps so a plan is never treated as fresh for
# ten seconds; LeRobot still owns queue creation and dequeue semantics.
runtime_n_action_steps: 2
input_timeout_ms: 2000.0
max_input_age_ms: 250.0
max_observation_skew_ms: 50.0
max_candidate_age_ms: 300.0
reset_gap_ms: 2000.0
action_units: radians
action_contract: arm7
state_dim: 7
action_dim: 7
image_shape:
- 480
- 640
output_topic: /act/right_arm_joint_control
state_topic: /robot1/right_arm/joint_states
diagnostics_topic: /act/right/diagnostics
state_key: observation.state
camera_keys:
  observation.images.main_rgb: /camera/camera/color/image_raw
  observation.images.auxiliary_rgb: /camera2/camera/color/image_raw
dataset_stats: ${TELEOP_DATA_ROOT}/converted/lerobot_task2_button_press_50hz/meta/stats.json
