# Connector insertion asset sources

The USB-C laptop insertion baseline uses only parameterized MuJoCo primitives.
It contains no downloaded mesh and therefore has no external asset license,
attribution, or SHA256 entry. Optional visual meshes must have one JSON manifest
per source using the fields required by `docs/connector-insertion-assets.md`,
and must remain `visual_only`.
