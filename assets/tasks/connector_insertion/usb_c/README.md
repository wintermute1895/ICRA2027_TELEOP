# USB-C parameter layer

The parameterized plug, receptacle, entry funnel, and insertion channel are
emitted by `tools/build_connector_insertion_scene.py` from the task JSON.
Their collision shapes are primitive MuJoCo geoms; no visual mesh is used for
collision.
