Collecting task-valid robot demonstrations is expensive when operators must
perform visually guided local corrections. We study a human-in-the-loop data
collection loop in which human correction intervals initialize visual auditing
and a bounded, vision-conditioned action model assists subsequent teleoperation.
This development draft records the intended claims and is not a result claim.
