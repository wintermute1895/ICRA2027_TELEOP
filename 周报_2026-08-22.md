# 本周工作总结（2026-08-18 ～ 2026-08-22）

## 一、本周完成

### 1. DexMimicGen → LeRobot ACT 仿真链路

- 完成 Robomimic HDF5 到 LeRobot 数据集的转换脚本，并完成 3 个 episode 的数据校验。
- 完成 ACT 最小训练、checkpoint 加载和 Robosuite rollout；当前 smoke test 已验证 action 24 维、state 14 维、20 Hz 控制频率、坐标系和动作顺序一致。
- 增加 GPU 训练脚本和 rollout 视频，修复了 rollout 视频方向问题。
- 第一阶段验收报告已整理完成。当前结论是基础设施链路打通，不代表策略成功率或大规模数据效果已经验收。

### 2. LBot 双臂遥操作和数据链路

- 完善驱动连接判断，兼容 SDK 返回首个有效句柄为 `id=0` 的情况；增加控制器信息和状态监控校验。
- 机械臂状态消息优先使用控制器时间戳，并增加接收时间回退和时钟重新锚定逻辑，改善相机/机械臂对齐。
- 对关节跟随指令增加“必须 7 个且全部为有限值”的输入校验。
- 在遥操作桥接层加入 One-Euro 滤波、左右臂独立方向映射、raw/filtered/mapped 三路诊断话题，并增加 `armed` 启动保护，避免未明确授权时向真机发送运动指令。
- 增加 D0 录包和自动分流脚本，记录相机、主臂、从臂状态、手部指令及配置 manifest。

### 3. 灵巧手控制

- 完成 L6/L10 的 open、pregrasp、grasp 三段式控制。
- 增加按名称切换和通过参数加载自定义手势的能力。
- 增加 L10 手势示教器及独立的手势播放/ROS 发布工具，手部指令可以和机械臂、相机一起录入 ROS bag。

## 二、遇到的问题及解决

1. **ROS2 接口构建失败。** 仓库路径包含中文目录，且 conda Python 版本与 ROS2 Humble 的 `rosidl`/`empy` 不兼容，导致接口生成阶段报错。已增加构建脚本：强制使用系统 Python 3.10，并将构建、安装目录放到纯 ASCII 路径 `/home/pao/icra2027_teleop_ws`。

2. **驱动误判连接失败。** SDK 文档把句柄 `id=0` 描述为无效，但当前 SDK 1.0.3 的第一个有效句柄确实可能为 0。已改为通过控制器信息查询和状态监控是否启动来判断连接是否有效。

3. **状态时间戳与录包时间对不上。** 原先状态发布定时器直接使用当前 ROS 时间，无法反映控制器采样时刻。已增加控制器时间戳到 ROS 时间的偏移映射，并保留无效时间戳设备的接收时间回退。

4. **遥操作抖动和误发指令风险。** 已在关节映射后、方向变换前加入可配置 One-Euro 滤波；同时增加输入维度/有限值检查和显式 `armed` 开关，并发布三路诊断话题用于定位延迟和映射问题。

5. **D0 录包的自动审计暴露出同步问题。** `d0_right_hand_003` 已成功录到相机、右臂和 L10 手话题，手部消息数量和关节数均符合要求，但 `camera_info` 最大 header/receipt 偏差为 **353.1 ms**，超过当前 250 ms 准入阈值，因此严格审计应进入 `A_audit`。仓库中另有一份在指定成功状态/放宽条件后生成的 `A_action` 报告，不能替代严格准入结果。这说明录包链路可用，但相机时间同步还没有达到动作数据准入标准。

## 三、目前卡点

1. **真机端到端验证尚未闭环。** 当前配置是右臂启用、左臂关闭；方向映射、滤波参数、首次同步和手臂/灵巧手联动仍需在明确安全条件下做完整回归，不能仅以编译通过或仿真 rollout 代替。

2. **D0 数据暂不能稳定进入 `A_action`。** `d0_right_hand_003` 的相机 `camera_info` 时间偏差超阈值；`d0_right_hand_002` 录制被中断，尚未生成完整的自动审计报告。需要先确定相机时间戳策略和录包准入阈值，再补录可复现样本。

3. **ACT 目前只有第一阶段 smoke test。** 训练只使用少量 episode 和少量步数，已验证工程链路，但尚未验证成功率、泛化性、数据规模效应或滤波器收益。

4. **构建环境仍依赖固定路径约束。** 构建脚本已经规避中文路径和 Python 版本问题，但还需要在干净环境中完成一次可复现构建，并把日常启动、录包和重建流程固定下来。

## 四、下周优先事项

- 在安全限位和 `armed` 保护下完成右臂遥操作、One-Euro 参数和方向映射回归，并确认是否恢复左臂测试。
- 修复/统一相机 header 时间戳与 ROS receipt 时间的对齐策略，补录至少一条通过自动审计的 D0 episode。
- 对已有 ACT checkpoint 做更长 rollout，并开始扩大数据量和正式评测，不把 smoke test 结果当作策略效果结论。
- 在干净 ASCII workspace 中完成一次完整构建，整理唯一推荐的启动和录包命令。
